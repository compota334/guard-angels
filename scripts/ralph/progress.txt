# Ralph Progress Log
Started: Tue Apr 28 04:57:10 AM CEST 2026
---

## Codebase Patterns
- Project uses ESM (`"type": "module"` in package.json). All imports need `.js` extensions for TypeScript NodeNext resolution.
- tsconfig: strict, ES2022, NodeNext module/resolution, outDir=dist, rootDir=src.
- Vitest configured with `passWithNoTests: true` in `vitest.config.ts` so zero-test runs exit 0.
- ESLint uses flat config (`eslint.config.js`) with `@eslint/js` + `typescript-eslint`.
- Verification commands: `npx tsc --noEmit`, `npx vitest run`, `npx eslint . --quiet`.
- tsconfig has `"types": ["node"]` — required for `process`, `Buffer`, etc.
- CLI entry: `src/bin/angels.ts` (shebang) → imports `src/cli.ts` (commander program). Build: `npm run build` → `dist/bin/angels.js`.
- ESLint enforces `preserve-caught-error` — always pass `{ cause: err }` when wrapping caught errors in new Error.
- Config loading: `src/config/load.ts` → `loadConfig(cwd)`. Schema: `src/config/schema.ts`. Defaults: `src/config/defaults.ts`.
- Path utilities: `src/paths/layout.ts` (canonical .angels/ paths), `src/paths/resolve.ts` (angel-id ↔ path conversion).
- Angel ID encoding: `/` → `-`, literal `-` → `--`. Root angel: `_root` ↔ `.`. This encoding is bijective (round-trips correctly).

---

## 2026-04-28T05:10:00Z - US-001: Inicializar proyecto npm + TypeScript
- Implemented: Bootstrap project with package.json (ESM, Node>=20, bin.angels), tsconfig.json (strict, ES2022, NodeNext), .editorconfig, vitest.config.ts, eslint.config.js, src/index.ts placeholder, README.md stub
- Files changed: package.json, package-lock.json, tsconfig.json, vitest.config.ts, eslint.config.js, .editorconfig, src/index.ts, README.md
- Verification: tsc=PASS  vitest=PASS  eslint=PASS
- Learnings for future iterations:
  - Vitest 4.x requires `passWithNoTests: true` in vitest.config.ts; otherwise exits 1 with no test files
  - Vitest 4.x does not support `--reporter=basic` — just use `npx vitest run` without custom reporter
  - typescript-eslint uses flat config via `tseslint.config()` helper
  - zod 4.x was installed (latest stable); API may differ from zod 3.x docs
---

## 2026-04-28T05:25:00Z - US-002: CLI entrypoint con todos los comandos stub
- Implemented: Created src/bin/angels.ts (shebang entry) and src/cli.ts (commander with all 10 commands as stubs). Added `types: ["node"]` to tsconfig.json for process global. Build produces dist/bin/angels.js with shebang preserved.
- Files changed: src/bin/angels.ts, src/cli.ts, tsconfig.json
- Verification: tsc=PASS  vitest=PASS  eslint=PASS
- Learnings for future iterations:
  - tsconfig needs `"types": ["node"]` explicitly for `process` to be available — NodeNext moduleResolution doesn't auto-include it
  - commander preserves shebang through tsc build (tsc copies it as-is from the first line)
  - Stub pattern: `console.error('not implemented: <name>'); process.exit(1);`
---

## 2026-04-28T05:40:00Z - BUG-001: Auditoría tras US-001 y US-002
- Implemented: No issues found in last 2 tasks. Verified: tsc=PASS, build=PASS, all 10 CLI stubs exit 1 with correct messages, vitest=PASS, eslint=PASS. No orphaned files, no debug logs, no missing scripts. README TODO is intentional per US-001 acceptance criteria.
- Files changed: scripts/ralph/prd.json, scripts/ralph/progress.txt
- Verification: tsc=PASS  vitest=PASS  eslint=PASS
- Learnings for future iterations:
  - Version string `.version('0.1.0')` is hardcoded in cli.ts — consider reading from package.json in a future task if drift becomes a concern
  - `src/index.ts` is a dead placeholder from US-001; will be repurposed or removed when real exports are added
---

## 2026-04-28T05:50:00Z - US-003: Carga y validación de _config.yml
- Implemented: Created src/config/{schema.ts,defaults.ts,load.ts}. Zod schema validates _config.yml structure exactly per section 6.1. loadConfig reads/parses YAML/validates with descriptive errors naming the offending field. defaults.ts exports canonical constants.
- Files changed: src/config/schema.ts, src/config/defaults.ts, src/config/load.ts, tests/unit/config.test.ts
- Verification: tsc=PASS  vitest=PASS (13 tests)  eslint=PASS
- Learnings for future iterations:
  - ESLint has `preserve-caught-error` rule — always pass `{ cause: err }` when re-throwing wrapped errors
  - Zod 4.x API is fully compatible with 3.x for object/string/number/enum/literal/array/min/optional
  - `z.infer<typeof Schema>` works as type-only construct in zod 4.x (no runtime `z.infer`)
  - Config module path: `src/config/` with schema.ts (Zod schema + types), defaults.ts (constants), load.ts (loadConfig function)
---

## 2026-04-28T05:30:00Z - US-004: Utilidades de paths y resolución de angel-id
- Implemented: Created src/paths/layout.ts (pure functions for all canonical .angels/ paths: angelsRoot, configFile, newspaperFile, briefsDir, responsesDir, inboxDir, outboxDir, locksDir, logsDir, cursorsDir, archiveDir, rootAngelDir, angelDir, angelMdFile, plus per-angel helpers). Created src/paths/resolve.ts (angelIdToPath, pathToAngelId, isRootAngel) with bijective encoding: `/` → `-`, literal `-` → `--`, `_root` ↔ `.`.
- Files changed: src/paths/layout.ts, src/paths/resolve.ts, tests/unit/paths.test.ts
- Verification: tsc=PASS  vitest=PASS (64 tests)  eslint=PASS
- Learnings for future iterations:
  - Angel ID encoding uses double-hyphen (`--`) to escape literal hyphens in path segment names. This makes the encoding bijective and round-trippable. E.g. `src/my-component` → `src-my--component`.
  - layout.ts functions take `projectRoot` (for top-level dirs) or `projectRoot + angelId/angelPath` (for per-angel dirs). The `angelDir`/`angelMdFile` functions take the angel's path (not ID), while `angelBriefsDir`, `angelLogsDir`, etc. take the angel ID.
---
