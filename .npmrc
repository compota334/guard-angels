audit=true
fund=false
audit-signatures=true
save-exact=true
engine-strict=true
# Skips dependency lifecycle scripts on install (supply-chain hardening).
# Also skips our own prepack/prepublishOnly, so publishing must go through
# `make publish` (or `npm publish --ignore-scripts=false`).
ignore-scripts=true
